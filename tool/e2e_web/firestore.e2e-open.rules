rules_version = '2';
// Regras abertas SOMENTE para seed/suite E2E Web local (emulator).
// Nunca deployar em produção.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
