/// Stub VM/nativo: `location.assign` só existe no Flutter Web.
void assignCheckoutLocation(String url) {
  throw UnsupportedError(
    'Navegação same-tab de checkout só está disponível no Flutter Web.',
  );
}
