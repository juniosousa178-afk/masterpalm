void publishCatStartTrace(List<Map<String, Object?>> events) {
  // No-op fora do Web (fallback temporario apenas para diagnostico no browser).
}

void publishCatStartSummary(Map<String, Object?> summary) {
  // No-op fora do Web (fallback temporario apenas para diagnostico no browser).
}
